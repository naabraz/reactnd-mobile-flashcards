import styled from 'styled-components/native'

import {
  h1Size,
} from './utils/fontSizes'

export const Title = styled.Text `
  margin-top: 50
  margin-bottom: 50
  font-size: ${h1Size}
  text-align: center;
`